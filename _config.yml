# The site itself is plain static HTML (index.html, projects/*.html) with no
# Jekyll templating -- this file only exists so GitHub Pages' Jekyll build
# picks up jekyll-sitemap and generates sitemap.xml automatically.
title: Manos Giannakis
description: >-
  Robotics and machine-learning engineer. Autonomous perception for Formula Student Driverless, and graph neural networks for neutrino event reconstruction.
url: "https://manosgiann15.github.io"

plugins:
  - jekyll-sitemap

exclude:
  - Gemfile
  - Gemfile.lock
  - README.md
  - node_modules
